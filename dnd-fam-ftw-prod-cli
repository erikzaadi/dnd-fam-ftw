#!/usr/bin/env bash
# Production management CLI wrapper. Full reference: MANAGE.md
#
# Usage: ./dnd-fam-ftw-prod-cli <resource> [sub-command] [args...]

set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$REPO_ROOT/scripts/deploy/dnd-fam-ftw-prod-cli" "$@"
